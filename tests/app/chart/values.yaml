replicaCount: 2

image:
  repository: myuser/helm-api  # REPLACE with your image
  pullPolicy: Never
  tag: "1.0.0"

service:
  type: ClusterIP
  port: 80
  targetPort: 5000

ingress:
  enabled: true
  className: "kong"
  host: "api.example.com"
  path: "/"
  pathType: Prefix
