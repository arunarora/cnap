apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "operator-autoremediationpolicies.fullname" . }}-test-connection"
  labels:
    {{- include "operator-autoremediationpolicies.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "operator-autoremediationpolicies.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
