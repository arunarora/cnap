apiVersion: v1
kind: Service
metadata:
  name: {{ include "operator-autoremediationpolicies.fullname" . }}
  labels:
    {{- include "operator-autoremediationpolicies.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "operator-autoremediationpolicies.selectorLabels" . | nindent 4 }}
