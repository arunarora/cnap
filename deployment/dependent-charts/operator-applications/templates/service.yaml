apiVersion: v1
kind: Service
metadata:
  name: {{ include "operator-applications.fullname" . }}
  labels:
    {{- include "operator-applications.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "operator-applications.selectorLabels" . | nindent 4 }}
