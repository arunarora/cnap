apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "operator-applications.fullname" . }}-test-connection"
  labels:
    {{- include "operator-applications.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "operator-applications.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
