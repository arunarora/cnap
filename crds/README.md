**IMPORTANT**

*Note: 1 : Start*
Always create the crd file name with the following format:
crd-<crd-plural-name>.yaml

Its important as the package script packages the CRD files using the REGEXP "crd-*.yaml" in the final helm-chart archive
*Note: 1 : End*

